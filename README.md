# Dom4j Element to Json

Simple Library to convert Dom4J element to Json (json-lib style)

# Usage

```java
String jsonText = Dom4jToJson.writeToString(element);
```

For more examples, please see the unit test.